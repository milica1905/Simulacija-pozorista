package application;

import java.io.IOException;
import java.net.URL;
import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.util.Optional;
import java.util.ResourceBundle;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;
import javafx.fxml.FXMLLoader;
import javafx.fxml.Initializable;
import javafx.scene.Node;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Alert;
import javafx.scene.control.Button;
import javafx.scene.control.ButtonType;
import javafx.scene.control.PasswordField;
import javafx.scene.control.TextArea;
import javafx.scene.control.TextField;
import javafx.stage.Stage;

public class Kontroler_O_Profilu_Scena implements Initializable {

	@FXML
	private Button nazad_dugme;

	@FXML
	private Button potvrda_nove_lozinke_dugme;

	@FXML
	private TextArea textArea_Info_O_Profilu;

	@FXML
	private PasswordField passwordField_Nova_lozinka;

	@FXML
	private PasswordField passwordField_Trenutna_lozinka;

	private Connection connect = null;
	private PreparedStatement statement = null;
	private String korisnicko = Kontroler_Radnik_prijava.vratiKorisnicko();
	public Pozoriste userRadnikPozoriste;
	public Radnik_Pozorista radnikpozorista;

	
	@FXML
    void promijeni_Lozinku(ActionEvent event) {
		Alert alert0 = new Alert(Alert.AlertType.CONFIRMATION);
	 	alert0.setTitle("POTVRDA");
	 	alert0.setContentText("Da li ste sigurni da zelite da promijenite lozinku");
	 	
	 	Optional <ButtonType>result = alert0.showAndWait();
	 	if(result.get()==ButtonType.OK) {
	 	
    	if(passwordField_Trenutna_lozinka.getText().equals("") && passwordField_Nova_lozinka.getText().equals("")) {
    		Alert alert1 = new Alert(Alert.AlertType.ERROR);
    		alert1.setTitle("UPOZORENJE");
			alert1.setContentText("Popunite polja");
			alert1.show();
    	}
    	else if(passwordField_Nova_lozinka.getText().equals("")) {
    		Alert alert3 = new Alert(Alert.AlertType.ERROR);
			alert3.setContentText("Unesite novu lozinku!");
			alert3.show();
    	}
    	else if(passwordField_Trenutna_lozinka.getText().equals("")) {
    		Alert alert2 = new Alert(Alert.AlertType.ERROR);
			alert2.setContentText("Unesite trenutnu lozinku!");
			alert2.show();
    	}
    	
    	else if(!Radnik_Pozorista.daLiJeDobroUnesenaSifra(Kontroler_Radnik_prijava.vratiKorisnicko(), Radnik_Pozorista.getMd5(passwordField_Trenutna_lozinka.getText()))) {
    		Alert alert3 = new Alert(Alert.AlertType.ERROR);
			alert3.setContentText("Netacno unesena lozinka za koriscniko ime:\n" + Kontroler_Radnik_prijava.vratiKorisnicko());
			passwordField_Trenutna_lozinka.setText("");
			passwordField_Nova_lozinka.setText("");
			alert3.show();
			
    	}
    	else if(Radnik_Pozorista.daLiJeDobroUnesenaSifra(Kontroler_Radnik_prijava.vratiKorisnicko(), Radnik_Pozorista.getMd5(passwordField_Trenutna_lozinka.getText())) && passwordField_Trenutna_lozinka.getText().equals(passwordField_Nova_lozinka.getText())) {
    		Alert alert3 = new Alert(Alert.AlertType.ERROR);
			alert3.setContentText("Koriscnice " + Kontroler_Radnik_prijava.vratiKorisnicko() + " unijeli ste opet staru lozinku za novu. Ako zelite da promijenite sifru unesite potpuno novu sifru!");
			alert3.show();
			passwordField_Nova_lozinka.setText("");
    	}
 
    	else {
    		connect = Konekcija.getConnection();
        	String sql = "UPDATE radnik_pozorista SET lozinka = ? WHERE id = ?";
        	try {
        		statement = connect.prepareStatement(sql);
        		statement.setString(1,Radnik_Pozorista.getMd5(passwordField_Nova_lozinka.getText()));
        		statement.setInt(2, Radnik_Pozorista.vratiIdTabele(Kontroler_Radnik_prijava.vratiKorisnicko()));
        		statement.execute();
        		
        		for(Radnik_Pozorista rp : Radnik_Pozorista.getLista_radnikaPozorista()) {
        			if(rp.getKorisnicko_ime().equals(Kontroler_Radnik_prijava.vratiKorisnicko()))
        				rp.setLozinka(Radnik_Pozorista.getMd5(passwordField_Nova_lozinka.getText()));
        		}
        		
        		Konekcija.ucitajRadnika();
        		Alert alert4 = new Alert(Alert.AlertType.INFORMATION);
        		alert4.setTitle("OBAVJESTENJE!");
        		alert4.setContentText("Uspjesno ste promijenili lozinku!");
        		Optional <ButtonType>result1 = alert4.showAndWait();
    		 	if(result1.get()==ButtonType.OK) {
    		 		try {
    					Parent root = FXMLLoader.load(getClass().getResource("Scena_nakonprijaveRadnika.fxml"));
    					Stage stage = (Stage)((Node)event.getSource()).getScene().getWindow();
    					Scene scene = new Scene(root);
    					stage.setTitle("Dobrodošli radniče " + Kontroler_Radnik_prijava.vratiImePrezime());
    					stage.setScene(scene);
    					stage.show();
    				}
    				catch(IOException e) {
    					e.printStackTrace();
    				}
    		 	}
        		
        		
        	
    	}
        	catch(Exception e) {
    		e.printStackTrace();
    	}
    	if (connect != null) {
			try {
				connect.close();
			} catch (SQLException e) {
				e.printStackTrace();
			}
		}
        
    	}
	 	}
	 	else {
	 		/*
	 
			*/
	 		
	 	}
	 }

	@FXML
	void switchto_Nakon_prijave_Radnika(ActionEvent event) {
		Alert upozorenje = new Alert(Alert.AlertType.CONFIRMATION);
		upozorenje.setTitle("POVRATAK NAZAD");
		upozorenje.setContentText("Molimo Vas da potvrdite da želite da se vratite na prethodnu stranicu.");

		Optional<ButtonType> result = upozorenje.showAndWait();
		if (result.get() == ButtonType.OK) {
			try {
				Parent root = FXMLLoader.load(getClass().getResource("Nakon_prijave_Radnika.fxml"));
				Stage stage = (Stage) ((Node) event.getSource()).getScene().getWindow();
				Scene scene = new Scene(root);
				stage.setTitle("Dobrodošli radniče " + Kontroler_Radnik_prijava.imeIPrezimeRadnika());
				stage.setScene(scene);
				stage.show();

			} catch (IOException e) {
				e.printStackTrace();
			}
		}
	}

	@Override
	public void initialize(URL arg0, ResourceBundle arg1) {
		// TODO Auto-generated method stub
		

	}
}